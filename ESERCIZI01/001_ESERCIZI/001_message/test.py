#TEST
import unittest
from message import Message

#TEST DOTTORE          
class TestMessage(unittest.TestCase):

    def setUp(self):
        self.name: str = "Alessandro"
    self.assertEqual(
        
    )
    res = Message.f_message(name)
    print (res)
    
    